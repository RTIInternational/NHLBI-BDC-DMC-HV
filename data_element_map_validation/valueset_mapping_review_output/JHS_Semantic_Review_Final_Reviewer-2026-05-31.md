# JHS Final Reviewer Packet — 2026-05-31
## Coverage
Reconciled 86 current JHS transforms against the fresh blind ledger, prior consolidated report, re-review summary, current YAML, and local JHS dbGaP dictionaries. Final packet promotes only findings confirmed against current transforms and local metadata; no YAML edits were made. Unverified CURIE-only concerns were excluded unless the source-variable semantics independently prove a collapse or mismatch.

## Final Confirmed Findings
| Priority | File | Final issue | Evidence to confirm | Recommended action | Confidence | Reviewer | Source alignment |
|---|---|---|---|---|---|---|---|
| P0 | `albumin_urine.yaml`, `crp.yaml`, `insulin_blood.yaml` | Character result fields and unsupported insulin conversion are emitted as numeric quantities. | Current YAML maps `phv00401742`, `phv00401732`, and `phv00401744` under `value_decimal`; JHS data dict identifies them as `umali_cresult`, `crphs2_cresult`, and `insr_cresult` with type `Char`. `insulin_blood.yaml` also emits `expr: "{phv00125954} * 6"` with no unit slot. | Remove `_cresult` PHVs from numeric `value_decimal`; retain numeric companion PHVs only. Disable or rebuild the `phv00125954` insulin conversion with a validated source unit, factor, and target unit. | CONFIRMED | Engineer | Both reviews |
| P0 | `glucose_bld.yaml` | Two glucose blocks divide values by 10 through `mg/L` to `mg/dL` conversion despite source descriptions and ranges indicating mg/dL-scale glucose. | Current YAML applies unit conversion to `phv00401716` and `phv00401733`. Data dict descriptions say “Glucose - mg/dL”; ranges `38-491` and `63-339` are mg/dL-like, while the XML unit tag says `mg/L`. | Treat these source values as mg/dL unless Anne/source owner contradicts; remove the automatic `mg/L` to `mg/dL` conversion and document the dbGaP unit-tag override. | CONFIRMED | Engineer | Re-review confirmed |
| P0 | `tak_diuret.yaml` | Insulin medication PHVs are mapped as diuretic exposure. | Current YAML maps `phv00401102`, `phv00401294`, and `phv00401574` to `ATC:C03`. Metadata identifies all three as diabetic insulin medication status. Correct diuretic PHVs in the analysis tables are `phv00401108`, `phv00401300`, and `phv00401401`. | Delete insulin-as-diuretic blocks; keep only true diuretic medication variables in this file. Insulin exposure belongs in `tak_insulin.yaml`. | CONFIRMED | Engineer | Both reviews |
| P0 | `visit.yaml`, `blood_pressure.yaml` | `JHS Exam 6` is created and used as a clinical exam label from contact occasion code 6. | Current `visit.yaml` creates `JHS Exam 6` from `pht008782`; current `blood_pressure.yaml` associates `pht008782` BP observations to `JHS Exam 6`. Metadata confirms contact occasion `VISIT=6`, but the prior/re-review layer confirms JHS clinical exams are not named Exam 6. | Do not preserve `JHS Exam 6` as a clinical exam label. Normalize after Anne confirms the code-6 crosswalk, likely to the canonical Exam 2 / Visit 6 representation. | HIGH | Architect | Re-review confirmed |
| P0 | `visit.yaml` | Incident/event tables are modeled as Visit entities with `visit_category: UNKNOWN`. | Current `visit.yaml` creates visits from `pht008728`, `pht008755`, and `pht008756`. Metadata shows event identifiers, incident stroke/HF fields, event/censoring year, and follow-up years rather than encounter records. | Remove or remodel event-table Visit rows; associate event conditions with a real visit, surveillance context, or event-time representation per architecture policy. | CONFIRMED | Architect | Re-review confirmed |
| P1 | `hist_cor_angio.yaml` | Carotid angioplasty, carotid endarterectomy, generic revascularization, and heart catheterization are mixed into a coronary angiography/procedure family. | Current YAML maps `CarotidAngioHx` PHVs `phv00401211`, `phv00401348`, `phv00401476` to the coronary procedure concept. Metadata also shows `phv00127731` is carotid endarterectomy and `phv00127740` is heart catheterization. | Split coronary angioplasty from carotid procedures and catheterization; remove carotid PHVs from the coronary angiography mapping or give them distinct procedure concepts. | CONFIRMED | Curator | Re-review confirmed |
| P1 | `qt_ekg.yaml`, `pr_ekg.yaml`, `qrs_ekg.yaml` | Distinct ECG interval measurements are collapsed without method/formula/lead qualifiers. | Current `qt_ekg.yaml` maps raw QT plus Framingham, Bazett, Hodge, and Fridericia QTc values to one observation type. `pr_ekg.yaml` maps per-lead PR durations to one PR interval type. `qrs_ekg.yaml` maps global and per-lead QRS durations to one QRS type. Data dict descriptions distinguish formulas and ECG leads. | Keep one representative global measurement, or add distinct observation types/qualifiers for correction formula and ECG lead before emitting multiple rows. | HIGH | Curator | Re-review confirmed |
| P1 | `ldl.yaml` | VAP LDL subfractions are collapsed into generic LDL cholesterol. | Current YAML maps `LDLC`, `LDLREAL`, `LDL1C`, `LDL2C`, `LDL3C`, and `LDL4C` from `pht008792` to the same LDL observation type. Metadata distinguishes each VAP component in mg/dL. | Keep generic LDL only for true `LDL-C`; split or qualify VAP subfractions, or omit subfraction blocks until approved terms exist. | HIGH | Curator | Both reviews |
| P1 | `cause_of_death.yaml` | Raw event death-classification text is assigned directly to `Person.cause_of_death`, with potential Person fan-out by event. | Current YAML maps `phv00401056` directly to `cause_of_death`. Metadata shows `pht008726` is keyed by `SUBJECT_ID` plus `event_id`, and `CFATALDX` is a `Char` death classification of an event. | Remodel as coded death/event evidence or a controlled cause-of-death mapping; avoid emitting repeated Person records from event rows. | CONFIRMED | Architect + Curator | Re-review confirmed |
| P1 | `fam_income.yaml` | Categorical income ranges and threshold yes/no items are modeled as `Quantity.value_concept` with `unit: USD`. | Current YAML places literal income bands under `value_concept` inside `Quantity`. Metadata shows encoded categorical income ranges and threshold questions, not numeric currency amounts. | Model as categorical SDOH values, value strings, or approved enum concepts; do not use `Quantity` unless a numeric amount is available. | CONFIRMED | Architect | Re-review confirmed |
| P1 | `vege_serving.yaml` | Dark-green vegetable servings are mapped as general vegetable intake. | Current YAML uses `phv00401250 * 7` for vegetable intake. Metadata identifies `phv00401250` as `darkgrnVeg`, “Daily Serving of Dark-green Vegetables.” | Either narrow the harmonized concept to dark-green vegetables or find a true total-vegetable source PHV; do not treat this PHV as total vegetable servings. | CONFIRMED | Curator | Re-review confirmed |
| P2 | `fam_stroke.yaml` | Family relationships are stored as OMOP relationship CURIEs in `relationship_to_participant`. | Current YAML uses values such as `OMOP:4277283`, `OMOP:4321888`, `OMOP:4218412`, and `OMOP:4326600`. BDCHM reference says `relationship_to_participant` expects `FamilyRelationshipEnum`; source metadata is family-history questionnaire content. | Replace with approved family relationship enum values such as mother, father, sibling, and offspring equivalents; align provenance to family-history policy. | CONFIRMED | Architect | Re-review confirmed |
| P2 | `ven_thromb.yaml` | AFU encoded values `A`, `K`, `M`, and `R` are silently dropped. | Current YAML maps only `N`, `U`, and `Y` for DVT and PE status. Data dict enumerates `A=Not applicable`, `K=Don't know`, `M=Missing`, `R=Refused`, `U=Unknown`, `Y=Yes`, `N=No`. | Map `K/M/R` to UNKNOWN or null per policy; adjudicate `A=Not applicable`; avoid emitting condition records with null status. | CONFIRMED | Engineer | Re-review confirmed |

## Anne Review Required
| Priority | File | Question | Evidence | Decision needed |
|---|---|---|---|---|
| P0 | Multiple visit-linked files | What is the authoritative JHS contact occasion / form version to clinical exam crosswalk for codes `1`, `6`, and `9`? | Metadata is internally inconsistent: `pht001945` table description says Visit 6 while variables say Visit 1; `pht008741`, `pht008737`, `pht008748`, and `pht008782` use code 6; `pht008783` has code 9 but description text includes Visit 1. | Confirm canonical labels for `cena`, `cenb`, `cenc`, `sbpb`, `sbpc`, `fsta`, `bcfa`, and `bcfv` before any broad visit-label remediation. |
| P1 | `blood_pressure.yaml` | Should JHS emit both raw BP readings and analysis-derived BP averages? | Current file emits multiple sitting BP readings/averages from BP forms plus analysis-derived BP rows for the same exams. | Decide whether to retain raw repeats, derived averages, or both with explicit method qualifiers. |
| P1 | Condition and procedure files | What provenance should “doctor said you had...” questionnaire, AFU, adjudicated event, and family-history sources use? | Reports identify inconsistent use of `CLINICAL_DIAGNOSIS`, self-report values, and family-history evidence across diabetes, heart failure, MI, family stroke, VTE, procedures, and AFU sources. | Set a JHS-wide provenance policy before bulk edits. |
| P1 | `spirometry.yaml`, `visit.yaml` | Should JHS spirometry explicitly state pre-bronchodilator state, and should `JHS PULMONARY` remain a Visit? | JHS spirometry appears pre-BD only; current YAML has no pre-BD qualifier and uses a `JHS PULMONARY` visit label. | Confirm pre-BD modeling and pulmonary visit architecture. |
| P2 | `mchc.yaml` | Should MCHC source `%` be represented as target `g/dL`? | Metadata reports MCHC as `%` with values numerically consistent with conventional g/dL-style MCHC; YAML asserts `g/dL` without conversion or note. | Confirm whether to keep `g/dL`, keep `%`, or document an intentional unit normalization. |
| P2 | `insulin_blood.yaml` | Is `phv00125954` actually micro-IU/mL, and what conversion factor should be used? | Metadata says `IU/mL` with range `1-981`, which is clinically consistent with micro-IU/mL rather than IU/mL; current YAML uses `* 6` and no unit. | Approve a source-unit override and factor, or omit the block. |

## Stale / Unsupported Prior Findings to Ignore
| Prior finding | Resolution | Evidence |
|---|---|---|
| All dbGaP `VISIT=6` tables mapped to `JHS Exam 2` are definitively wrong and should become `JHS Exam 6`. | Ignore as a direct remediation instruction; route to Anne crosswalk instead. | Current metadata shows internal contact occasion code 6, while prior/re-review confirms `JHS Exam 6` is not a valid clinical exam label. |
| `fast_gluc_bld.yaml` shares the HDL observation type or contains the glucose divide-by-10 bug. | Ignore. | Current `fast_gluc_bld.yaml` uses fasting glucose PHVs with mg/dL units; the divide-by-10 issue is in `glucose_bld.yaml`, not `fast_gluc_bld.yaml`. |
| JHS race hardcoding to Black/African American is a semantic error. | Ignore for this reviewer packet. | JHS cohort context supports African American race coding; fresh blind review also found core demography context coherent. |
| Broad CURIE-suspicion findings without local verification. | Ignore until Curator performs terminology-cache or Athena/OBO verification. | Prior reports explicitly marked many OBA/OMOP/HP/MONDO claims as unverified; this packet includes only source-semantic collapses or verified metadata mismatches. |
| `pht008742/cenc` is definitely clean as Visit 1, or definitely Exam 3 based only on table mnemonic. | Do not act directly; include in Anne crosswalk. | Metadata for the table is ambiguous and review inputs conflict. Current YAML maps it to Exam 1, while reports disagree on whether `cenc` should follow form version C or visit-description text. |
| Drug provenance spacing should be treated as a top semantic finding. | Defer to lint/schema remediation, not this semantic reviewer packet. | The malformed `PATIENT SELF-REPORTED MEDICATION` value is real and likely lint-covered, but it is not one of the fewer high-impact semantic findings prioritized here. |

## Reviewer Checklist
- Fix deterministic P0 source-type/unit/PHV errors before broad visit edits.
- Do not bulk-change JHS visit labels until Anne confirms the contact occasion crosswalk.
- For each `_cresult` removal, verify the numeric companion PHV remains active.
- For ECG, LDL VAP, and procedure-family fixes, preserve source specificity through qualifiers or distinct concepts rather than swapping to unverified CURIEs.
- After YAML remediation, run HV-Lint Phases 1, 2, 3, and 5 with JHS visit cache.
- Log lint/audit gap candidates for `_cresult` numeric use, unit-tag/range contradictions, wrong medication-family PHVs, event tables modeled as visits, categorical values under `Quantity`, and dropped sentinel codes.

## Reconciliation Notes
Fresh blind and prior/re-review agree on the strongest deterministic issues: `_cresult` numeric mappings, insulin-as-diuretic, and LDL subfraction collapse. Those were retained.

Prior/re-review-only findings were promoted only when current YAML plus local metadata confirmed the live defect. This is why glucose scaling, event-table visits, procedure-family mixing, ECG collapse, income-as-quantity, VTE sentinel handling, and dark-green vegetable misclassification remain in the final table.

The main conflict is JHS visit interpretation. The fresh ledger treated `VISIT=6` as literal Visit 6, while prior/re-review treated it as an internal contact code that should not become `JHS Exam 6`. Current metadata supports the existence of code 6 but not the clinical label `Exam 6`; therefore broad visit reassignment is Anne-gated, while the fabricated `JHS Exam 6` label remains a confirmed architecture risk.

No unverified standalone CURIE concerns were included.
