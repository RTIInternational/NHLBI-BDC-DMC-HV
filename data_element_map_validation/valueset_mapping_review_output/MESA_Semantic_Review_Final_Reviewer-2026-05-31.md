# MESA Final Reviewer Packet — 2026-05-31

## Coverage

Reviewed the current 123-file `MESA-ingest` transform inventory against the fresh blind ledger, prior consolidated report, and re-review summary. Current YAML was inspected directly for the retained conflicts and high-impact findings. Local dbGaP MESA metadata, visit cache, MONDO cache, and OMOP terminology cache were used where needed. No YAML edits were made.

## Final Confirmed Findings

| Priority | File | Final issue | Evidence to confirm | Recommended action | Confidence | Reviewer | Source alignment |
|---|---|---|---|---|---|---|---|
| P1 | blood_pressure.yaml; bp_systolic.yaml; bp_diastolic.yaml | SBP/DBP are emitted twice: once inside grouped BP sets and again as standalone observations. | Current YAML reuses the same PHVs across files, including `phv00083410`/`phv00083411`, `phv00085740`/`phv00085741`, and sibling visit pairs; dbGaP defines these as seated BP in `mmHg`. | Keep one representation, preferably grouped BP if that is the target model pattern, and suppress/delete standalone duplicates. | CONFIRMED | Engineer | Both reviews |
| P1 | insulin_blood.yaml; il6.yaml; eselectin.yaml | Nested `Quantity.populated_from` and/or value expressions bind PHVs from the wrong table. | Current YAML shows outer tables such as `pht002198`, `pht003091`, `pht002105`, `pht004326`, `pht001116`, and `pht001120` with nested Quantity rows still bound to unrelated tables; dbGaP places the PHVs in the outer/source tables. | Align every nested `Quantity.populated_from` and value PHV with the enclosing source table, or add explicit supported joins where truly cross-table. | CONFIRMED | Engineer | Both reviews |
| P1 | hist_cor_bypg.yaml | Family CABG block reads status from an Air Exam PHV. | Current `pht001121` block uses `phv00082876`; dbGaP places `phv00082876` in `pht001111` and identifies `phv00087146` in `pht001121` as “CORONARY BYPASS SURGERY” with `0/1/9`. | Change the Family block status PHV to `phv00087146` and preserve `9 = UNKNOWN`. | CONFIRMED | Engineer | Both reviews |
| P1 | creat_bld.yaml | Serum creatinine uses an invalid/wrong OMOP concept. | Current YAML uses `OMOP:2212294`; dbGaP defines `phv00175241` and `phv00176664` as serum creatinine in `mg/dL`; local OMOP has no concept ID `2212294`, while `OMOP2212294` is a concept code for clofibrate and `OMOP:3016723` is serum/plasma creatinine mass/volume. | Replace with a verified serum/plasma creatinine measurement concept, likely `OMOP:3016723` after Curator sign-off. | CONFIRMED | Curator | Fresh-only |
| P1 | creat_urin.yaml | Urinary creatinine concentration uses an interpretation concept. | Current YAML uses `OMOP:3007081`; dbGaP PHVs are numeric urinary creatinine in `mg/dL`; local OMOP labels `3007081` as “Creatinine [Interpretation] in Urine” and `3017250` as “Creatinine [Mass/volume] in Urine.” | Replace with verified urine creatinine mass/volume concept, likely `OMOP:3017250`. | CONFIRMED | Curator | Fresh-only |
| P1 | cac_score.yaml; cac_volume.yaml | CAC score/volume files conflate distinct CT-derived quantities; CAC volume also carries a malformed HU unit literal. | Current `cac_score.yaml` maps multiple unadjusted, phantom-adjusted, and volume-score PHVs to one Agatston concept. Current `cac_volume.yaml` repeats `#reported_units: "Hounsfield units (HU)` as the unit for volume PHVs; HU is attenuation, not volume. | Select canonical Agatston and calcium-volume endpoints, split or suppress non-comparable derived variants, and remove the malformed HU unit. | CONFIRMED | Curator | Both reviews |
| P1 | afib.yaml | AFib conditions are duplicated within the same visit, and unconfirmed ECG values are mapped to `PRESENT`. | Current `pht001111` emits both `phv00082701` and computed `phv00082843` as AFib for the same visit; `pht003091` emits both `phv00176281` and `phv00176292`; value `2` is also mapped to `PRESENT` in ECG-confirmation blocks. | Use one source per visit, preferably computed/confirmed where available; send unconfirmed status semantics to Anne/Curator if not already decided. | CONFIRMED | Curator | Re-review confirmed |
| P1 | chd.yaml | Hard CHD and all CHD are emitted as the same condition concept/status. | Current YAML maps `phv00087886` hard CHD and `phv00087888` all CHD from `pht001123` to `MONDO:0005010` with `HISTORICAL` status. | Split the endpoints or retain only the intended canonical CHD endpoint to avoid duplicate/conflated events. | CONFIRMED | Curator | Re-review confirmed |
| P1 | hyperten.yaml | Computed hypertension flags are labeled self-report, and an Exam 2 family-history PHV is mapped as participant’s own hypertension. | Current YAML maps computed JNC-VI PHVs (`phv00082655`, `phv00084459`, `phv00087094`) with self-report provenance; `pht001117/phv00085762` is from the Exam2FamilyHx table but uses `relationship_to_participant: ONESELF`. | Mark computed hypertension as derived/clinical-protocol evidence; remodel `pht001117` as family history rather than participant condition. | CONFIRMED | Curator | Re-review confirmed |
| P1 | nt_bnp.yaml | Exam 1 and Exam 3 NT-proBNP values collapse to one visit label. | Current YAML maps both `phv00156621` and `phv00156622` to `"MESA BNP"`; dbGaP says `pht002140` contains Exam 1 and Exam 3 NT-proBNP. | Use distinct associated visits for Exam 1 and Exam 3 NT-proBNP or otherwise preserve exam identity. | CONFIRMED | Engineer | Re-review confirmed |
| P1 | potassium.yaml | Exam 4 potassium block references Exam 3 age PHV without a join. | Current `pht001120` block uses `phv00191192` to distinguish Exam 3 vs Exam 4 draw, but `age_at_observation` references `phv00086259` from `pht001119` with no join. | Add a supported join for Exam 3 age or use an in-table/derived age strategy that does not cross tables implicitly. | CONFIRMED | Engineer | Re-review confirmed |
| P1 | tnfa.yaml | `value_decimal` can return string literals `LLOD` and `ULOD`. | Current YAML returns `"LLOD"`/`"ULOD"` when `phv00160541` has missing-reason codes; dbGaP defines `phv00160541` as a missing-value-reason variable, not a numeric TNF-alpha result. | Null/suppress numeric value for detection-limit rows and represent limits/qualifiers separately if supported. | CONFIRMED | Engineer | Re-review confirmed |
| P2 | demography.yaml | Lung CT demography race logic is lossy and inconsistent with other MESA race/ethnicity blocks. | Current `pht003087` race expression only emits Black (`3`) and duplicates branches; dbGaP race variables include codes `1=White`, `2=Chinese American`, `3=Black`, `4=Hispanic`; other MESA blocks handle code `4` through ethnicity and race policy. | Rewrite Lung CT race/ethnicity logic to follow the agreed MESA convention and remove duplicated branches. | HIGH | Anne | Both reviews |
| P2 | cd40.yaml | CD40 ligand source is mapped to a CD40 receptor/substance concept. | Current YAML uses `OMOP:4209737`; dbGaP `phv00085021` is “CD40 LIGAND (pg/mL)”; local OMOP labels `4209737` as “Lymphocyte antigen CD40” and `42872773` as “Lymphocyte antigen CD154.” | Do not keep CD40 receptor as-is; choose a verified CD40 ligand/CD154 measurement or approved fallback concept. | HIGH | Curator | Both reviews |
| P2 | ven_thromb.yaml | Same blood-clot construct is split across broad thrombotic disease and venous thromboembolism concepts. | Current YAML maps Air/Exam 1 blood clots in legs/lungs to `MONDO:0000831`; Family blood clots in lung or leg veins maps to `MONDO:0005399`; local MONDO labels these as thrombotic disease vs venous thromboembolism. | Harmonize to one intended concept, likely VTE if Anne/Curator agrees the source construct is venous leg/lung clot history. | HIGH | Curator | Fresh-only |

## Anne Review Required

| Priority | File | Question | Evidence | Decision needed |
|---|---|---|---|---|
| P1 | pacem_stat.yaml | Should body-composition scale pacemaker safety-screen PHVs be included as pacemaker status, and if included should they keep ECG provenance? | Fresh review found `phv00174642` and `phv00176065` are “BODY COMPOSITION SCALE: DOES PPT HAVE A PACEMAKER?” while `phv00082702` is true ECG pacemaker and `phv00087149` is self-reported implant. | Keep/split/drop safety-screen sources and set the correct method/provenance. |
| P1 | stroke.yaml | Should stroke subtype code `9 = UNKNOWN STROKE TYPE` map to generic stroke or be omitted as subtype while retaining stroke status? | `phv00087877` carries subtype, while stroke status is carried separately by `phv00087876`; current YAML maps subtype `9` to generic stroke. | Confirm target semantics for unknown subtype. |
| P2 | demography.yaml | For MESA race code `4 = Hispanic`, should harmonized race be `UNKNOWN` with ethnicity populated, or should race also retain Hispanic? | Most current MESA blocks map code `4` to ethnicity and race `UNKNOWN`; Lung CT block currently needs rewrite and should follow the agreed policy. | Confirm cohort-wide race/ethnicity convention before final demography remediation. |
| P2 | spirometry.yaml | Should pre- vs post-bronchodilator spirometry be explicitly encoded? | Prior/re-review indicate MESA Lung includes post-BD spirometry while AIR/Family are likely pre-BD; current blocks lack a discriminator. | Decide required method/qualifier pattern and source-specific BD assumptions. |
| P2 | lppla2_act.yaml; lppla2_mass.yaml | Are the current OMOP concepts correct for Lp-PLA2 activity vs mass? | Source PHVs and units distinguish mass (`ng/mL`) from activity (`nmol/min/mL`), but concept specificity still needs expert OMOP confirmation. | Approve current concepts or select verified replacements. |
| P2 | tak_*.yaml | What is the medication vocabulary/class policy for MESA drug-class files? | Current files mix ATC, RxCUI, NDFRT, VANDF, and MeSH; generic class PHVs sometimes emit product-specific or multiple exposure records. | Decide class-vs-product modeling and allowed vocabularies before bulk medication cleanup. |
| P2 | vege_serving.yaml | Is `(sum / 30) * 7` the correct FFQ food-group conversion? | Source Exam 5 FFQ variables are computed food groups with units not explicit in dbGaP; prior report could not confirm whether values are daily-equivalent or 30-day frequencies. | Confirm source unit convention and per-week conversion. |
| P3 | visit.yaml | Is `visit_category` still accepted on this branch, or should MESA visits use `visit_type`? | Prior/re-review flagged a possible schema migration issue; this depends on the deployed bdchm version. | Confirm current schema expectation or rely on HV-Lint Phase 2. |

## Stale / Unsupported Prior Findings to Ignore

| Prior finding | Resolution | Evidence |
|---|---|---|
| `creat_bld.yaml` missing `age_at_observation` | Stale | Current YAML has `age_at_observation` for both serum creatinine blocks. |
| `hist_hrtdis.yaml` / `hist_hrtfail.yaml` content swap | Stale | Re-review states the swap finding no longer applies to current transforms. |
| `pht003088` Lung race group is missing White/Chinese mappings | Unsupported | Re-review states `pht003088` `racegrp` only has Hispanic and Black in current local metadata; do not assign a White/Chinese coverage fix from that table. |
| `carotid_plaque.yaml` is Exam 1 only | Stale | Re-review states current `carotid_plaque.yaml` is no longer Exam 1 only. |
| Missing MESA Exam 6 visit/transforms | Unsupported | Local MESA v13.p3 metadata and re-review confirm no Exam 6 tables are expected. |
| Bulk “verify all OMOP/OBA CURIEs” finding | Not actionable as a reviewer packet item | This packet retains only terminology issues verified against local cache or source metadata; unverified broad CURIE sweeps should not be remediated from the prior report alone. |
| General coverage gaps without source-table confirmation | Defer | Several prior rows assert missing routine exam coverage; retain only after dbGaP table/PHV existence and priority scope are confirmed. |

## Reviewer Checklist

- [ ] Address P1 row-binding defects first: `insulin_blood.yaml`, `il6.yaml`, `eselectin.yaml`, `hist_cor_bypg.yaml`, and `potassium.yaml`.
- [ ] Resolve duplicate/event inflation next: BP, AFib, CHD, CAC score/volume, and NT-proBNP visit collapse.
- [ ] Apply verified terminology fixes for creatinine and CD40L only after Curator sign-off on replacement concepts.
- [ ] Route Anne-policy items before bulk changes: MESA race/ethnicity, pacemaker safety-screen provenance, stroke subtype unknowns, spirometry BD semantics, and medication class policy.
- [ ] Re-run HV-Lint phases 1, 2, 3, and 5 after YAML remediation, with visit cache enabled for Phase 5.
- [ ] Do not resurrect stale prior findings listed above.

## Reconciliation Notes

Fresh blind findings were favored when they were independently verified against current YAML and local metadata. Prior-only findings were retained only when the re-review also confirmed them or direct current YAML inspection showed the defect still exists.

The final list intentionally excludes broad “verify CURIE” items, unsupported coverage guesses, and stale findings. The strongest retained classes are deterministic row-resolution bugs, duplicate downstream records, verified wrong concepts/units, and visit/PHV mismatches that can be fixed without clinical guesswork.
