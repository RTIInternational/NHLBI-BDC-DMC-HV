# Block 0: ECG-detected atrial fibrillation (Novacode Major6 classification)
# phv00226270 (ECGA271) is a string-presence variable: "Atrial fibrillation" when detected, null otherwise
- class_derivations:
    Condition:
      populated_from: pht004715
      slot_derivations:
        associated_participant:
          expr: 'uuid5("https://w3id.org/bdchm/Participant", str({phv00226250}) + ":HCHS")'
        associated_visit:
          expr: 'uuid5("https://w3id.org/bdchm/Visit", str({phv00226250}) + ":HCHS EXAM")'
        condition_concept:
          value: MONDO:0004981
          range: string
        condition_status:
          expr: 'case(({phv00226270} == ''Atrial fibrillation'', ''PRESENT''), (True, ''ABSENT''))'
        condition_provenance:
          value: STUDY_RECORD
          range: string
        associated_evidence:
          value: "12-lead ECG"
          range: string
        relationship_to_participant:
          value: ONESELF
          range: string
# Block 1: Self-reported atrial fibrillation history
# phv00526613 (MHEA7) — binary 0=No, 1=Yes
- class_derivations:
    Condition:
      populated_from: pht004715
      slot_derivations:
        associated_participant:
          expr: 'uuid5("https://w3id.org/bdchm/Participant", str({phv00226250}) + ":HCHS")'
        associated_visit:
          expr: 'uuid5("https://w3id.org/bdchm/Visit", str({phv00226250}) + ":HCHS EXAM")'
        condition_concept:
          value: MONDO:0004981
          range: string
        condition_status:
          populated_from: phv00526613
          value_mappings:
            '0': ABSENT
            '1': PRESENT
            'Q': UNKNOWN
        condition_provenance:
          value: PATIENT_SELF_REPORT
          range: string
        associated_evidence:
          value: "self-report questionnaire"
          range: string
        relationship_to_participant:
          value: ONESELF
          range: string
