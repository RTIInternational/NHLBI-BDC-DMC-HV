# uv-managed virtual environment
.venv/

# Run outputs written by the wrapper scripts (aggregate summaries + reports)
runs/

# TOPMed tar extraction scratch (when --base-dir is writable)
extracted/

# Python bytecode
__pycache__/
*.pyc
